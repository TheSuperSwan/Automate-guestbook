apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-frontend-gha
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guestbook-frontend-gha
  template:
    metadata:
      labels:
        app: guestbook-frontend-gha
    spec:
      containers:
        - name: frontend
          image: ghcr.io/thesuperswan/guestbook-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-frontend-gha
spec:
  selector:
    app: guestbook-frontend-gha
  ports:
    - name: http
      port: 8080
      targetPort: 8080

